<nav class="nav-links" aria-label="site navigation">
  <a href="/blog" class="nav-links__link blog-link">Blog</a>
  <a href="/contacts" class="nav-links__link contacts-link">Contacts</a>
</nav>

<style lang="scss">
  .nav-links {
    display: none;
    gap: 0.5rem;

    &__link {
      text-decoration: none;
      color: inherit;
      padding: 0.25rem 0.75rem;
      border-radius: 4px;
      font-weight: 500;
      transition: all 0.2s ease;

      &:hover {
        background-color: var(--clr-secondary);
        color: #fff;
      }
    }
    .contacts-link {
      background-color: var(--clr-accent);
      color: #fff;
    }
  }

  @media screen and (min-width: 636px) {
    .nav-links {
      display: flex;
    }
  }
</style>
